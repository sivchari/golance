// Package other is typehierarchy's fixture for a cross-package type name
// (see typehierarchy.go's Var).
package other

// Remote is declared in a different workspace package than the query.
type Remote struct{}
